var arrayInts = [1,2,3,4,5,6]

function highestProduct(array) {
  //step through with the greedy paradigm
  array.forEach(function(element, index) {
    
    //largest positive number check

    //check if positive or negative


  })
    //keep track of following variables
      /*
        largest positive integer thus far
        product of two negative numbers
        product of two highest positive numbers
      */

}