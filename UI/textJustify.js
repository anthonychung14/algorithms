/**
 * @param {string[]} words
 * @param {number} maxWidth
 * @return {string[]}
 */
var fullJustify = function(words, maxWidth) {
    
    //iterate over collection
    //instantiate newLine
    //if(checkCurrentWord(words[i], maxWidth, currLine)) {add to line}
    //else => processLine
    
    var checkCurrentWord = function(words, maxWidth, currLine) {

    
    };
    
    var processLine = function(words, maxWidth) {

    
    };
    
};

